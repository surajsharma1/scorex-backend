#!/usr/bin/env node\n/**\n * Render-safe pre-build script: Ensure public/overlays exists with files\n * Skip sibling copy on Render (not present), use local files\n */\n\nconst fs = require('fs');\nconst path = require('path');\n\nconst overlaysDir = path.join(__dirname, '../public/overlays');\n\n// Ensure target dir exists\nif (!fs.existsSync(overlaysDir)) {\n  fs.mkdirSync(overlaysDir, { recursive: true });\n  console.log('📁 Created public/overlays/');\n}\n\n// Check if overlays have content\nif (fs.readdirSync(overlaysDir).length === 0) {\n  console.warn('⚠️  public/overlays empty - add overlay files manually or sync from frontend');\n} else {\n  console.log(`✅ public/overlays ready with ${fs.readdirSync(overlaysDir).length} files`);\n}\n\nconsole.log('🎬 Pre-build overlays check complete');
